// Package onboarding manages the server joining flow configuration. Settings control the welcome channel, verification
// requirements, open-join behaviour, minimum account age, and automatic role assignments. Onboarding documents (such as
// rules or guidelines) are stored on the filesystem via the DocumentStore interface.
package onboarding
