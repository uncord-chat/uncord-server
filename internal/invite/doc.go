// Package invite provides domain types and validation for server invite codes. Invites support optional expiration by
// time and by use count. The Repository interface defines creation, lookup, consumption, and listing operations backed
// by PostgreSQL.
package invite
