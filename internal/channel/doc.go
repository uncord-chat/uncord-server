// Package channel provides domain types and validation for server channels. Supported types include text, voice,
// announcement, forum, and stage. Channels belong to optional categories and carry per-channel settings such as slowmode
// intervals, topic text, and NSFW flags. The Repository interface defines CRUD operations backed by PostgreSQL.
package channel
