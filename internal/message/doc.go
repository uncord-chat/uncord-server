// Package message provides domain types and validation for chat messages. Messages belong to channels and carry
// content, author references, optional reply targets, a pinned flag, and soft-delete tracking. The Repository interface
// defines CRUD and query operations backed by PostgreSQL.
package message
