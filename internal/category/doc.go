// Package category provides domain types and validation for channel categories. Categories group channels into named,
// ordered sections within the server. The Repository interface defines CRUD operations backed by PostgreSQL.
package category
