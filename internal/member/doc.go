// Package member provides domain types and validation for server membership records. A member represents a user who has
// joined the server and carries a status, optional nickname, timeout state, and role assignments. The package also
// handles ban records. The Repository interface defines CRUD and query operations backed by PostgreSQL.
package member
